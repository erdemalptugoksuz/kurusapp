/**
 * Application-wide constants
 * Centralized location for shared constants to maintain DRY principle
 */

// Key for persisting recovery mode state in AsyncStorage
// Used by AuthContext and deepLinking modules
export const RECOVERY_MODE_KEY = "kurusapp_recovery_mode";

